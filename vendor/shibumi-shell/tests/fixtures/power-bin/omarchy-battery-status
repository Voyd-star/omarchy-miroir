#!/usr/bin/env bash

set -euo pipefail

[[ ${1:-} == --shell ]] || exit 2
printf '%s\n' \
  $'percentage\t40%' \
  $'state\tcharging' \
  $'rate\t36.3W' \
  $'size\t50Wh' \
  $'time\t49m' \
  $'cycles\t102' \
  $'threshold\t75-80%'
